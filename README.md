# Next Static Site

Minimal Next.js starter for a static content website.

## Run locally

```bash
npm install
npm run dev
```

## Folder structure

- `pages/` for routes
- `lib/` for content helpers
- `content/` for Markdown files
